<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:name="io.github.rool.chatCloneCompose.ChatCloseApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_middle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_middle"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chatclonecompose"
        tools:targetApi="31" >
        <activity
            android:name="io.github.rool.chatCloneCompose.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Chatclonecompose" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>